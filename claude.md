# HTML 변경 후 검증 체크리스트

이 프로젝트는 단일 `index.html` 정적 페이지다. HTML, CSS, JavaScript, 일정 데이터, 지도 데이터를 변경한 뒤에는 아래 항목을 확인한다.

## 기본 검증

- `claude.md`와 사용자가 준 지시를 먼저 읽는다.
- `<script>` 블록을 추출해 JavaScript 문법 검사를 실행한다.
- 로컬 서버로 `index.html`을 열어 브라우저에서 실제 동작을 확인한다. `file://` 검증만으로 끝내지 않는다.
- 브라우저 콘솔 에러가 없는지 확인한다.

## 일정/카드 데이터 검증

- 새 장소를 추가하면 일정 카드와 지도 마커 양쪽에 모두 노출되는지 확인한다.
- 식당은 `태기추천` 요청이 있으면 tags 배열에 반드시 포함한다.
- `점포 확인`, `점포별 상이`처럼 비어 보이는 문구를 남기지 않는다. 영업시간을 정확히 못 박기 어려운 경우에도 대표 메뉴와 대략 가격대를 넣는다.
- 영업시간, 휴무, 가격은 브라우저로 공식 사이트, Google Maps, 예약/메뉴 페이지를 열어 확인한다.
- 사용자가 준 Google Maps 단축 링크가 실제 장소가 맞는지 브라우저로 열어 최종 URL과 장소명을 확인한다.

## Google Maps 링크 검증

- 새 장소의 `구글 지도에서 열기` 링크는 브라우저로 직접 눌러 장소 패널이 뜨는지 확인한다.
- `placeId` 또는 `ftid`로 빈 지도만 뜨면 그대로 두지 말고, 좌표 링크가 아닌 장소명 검색 링크(`mapQuery`) 등 정상 동작하는 링크로 바꾼다.
- 동식물원처럼 큰 부지의 좌표는 사용자가 준 링크만 믿지 말고 Google Maps에서 장소명을 직접 검색해 대표 장소 좌표를 확인한다.

## 지도 마커 검증

- 각 Day 탭과 A/B안 토글을 브라우저에서 직접 눌러 마커 수와 표시 상태를 확인한다.
- 메인 일정 마커와 서브 마커가 같은 좌표 또는 같은 장소 식별자를 가질 때 서브 마커가 누락되지 않는지 확인한다.
- 새로 추가한 식당/명소가 지도에 서브 마커로 보이고, 카드 클릭 팝업도 열리는지 확인한다.
- 팝업의 `지도에서 위치 보기` 버튼을 눌러 지도 이동과 장소명 툴팁 표시를 확인한다.

## 완료 전 보고

- 검증한 브라우저 동작을 요약한다.
- 확인 중 이상했던 링크, 불확실한 가격/영업시간, 공식 사이트 접근 문제는 숨기지 않고 보고한다.
- 작업 완료 후 한국어 커밋 메시지로 커밋한다. Co-Authored-By 서명은 추가하지 않는다.
